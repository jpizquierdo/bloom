# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Virtualenv / uv
.venv/

# Environment
.env

# OS / editor
.DS_Store
.idea/
.vscode/

# Compiled web UI, produced by the Docker build (or a local `npm run build -- --outDir`)
bloom/static/

# Claude Code (personal/local settings; a shared .claude/settings.json may be committed)
.claude/settings.local.json
# Agent skills are regenerated per-machine (uvx library-skills); symlinks point into .venv.
.claude/skills/

# One-off bootstrap prompt (not part of the project)
CLAUDE_CODE_PROMPT.md
